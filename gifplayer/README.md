# GifPlayer plugin

The B4J source implements a WAN-stable AWTRIX2 GIF player with a 4 FPS cadence and a device-side 120-second hard timeout. Build it with the AWTRIX2 B4J application toolchain and install the resulting plugin on the Host.

